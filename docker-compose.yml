services:
  # PostgreSQL Database for XATSimplified
  postgres:
    image: postgres:12.2
    container_name: perfanalysis-postgres
    environment:
      POSTGRES_DB: perfanalysis
      POSTGRES_USER: perfadmin
      POSTGRES_PASSWORD: devpassword123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U perfadmin"]
      interval: 10s
      timeout: 5s
      retries: 5

  # XATSimplified Django Application
  xatsimplified:
    image: xatsimplified-web:latest
    container_name: perfanalysis-xatsimplified
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./XATSimplified:/app
      - ~/.oci:/root/.oci:ro  # Mount OCI config for cloud provider integration
      - ~/.ssh:/root/.ssh:ro  # Mount SSH keys for remote deployment
    ports:
      - "8000:8000"
    env_file:
      - ./XATSimplified/.env
    environment:
      - DEBUG=True
      - DATABASE_URL=postgresql://perfadmin:devpassword123@postgres:5432/perfanalysis
      - SECRET_KEY=dev-secret-key-change-in-production
      - DJANGO_SETTINGS_MODULE=core.settings
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # perfcollector2 daemon (pcd)
  pcd:
    build:
      context: ./perfcollector2
      dockerfile: Dockerfile.dev
    container_name: perfanalysis-pcd
    command: /app/bin/pcd
    volumes:
      - pcd_data:/var/lib/pcd
    ports:
      - "8080:8080"
    environment:
      - PCD_LISTENADDRESS=0.0.0.0:8080
      - PCD_HOME=/var/lib/pcd
      - PCD_LOGLEVEL=DEBUG
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/v1/ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  # perf-dashboard React Frontend (run separately with npm run dev)
  # Visualization is handled by React/Chart.js in perf-dashboard
  # No R components - all visualization in frontend

volumes:
  postgres_data:
  pcd_data:

networks:
  default:
    name: perfanalysis-network
